import { ServicesClient } from "@/components/public/services/ServicesClient";

export const metadata = {
  title: "Services — Mekiya Real Estate",
};

export default function ServicesPage() {
  return <ServicesClient />;
}
